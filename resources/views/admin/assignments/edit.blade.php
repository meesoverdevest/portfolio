@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">Edit assignment {{ $assignment->id }}</div>
                    <div class="panel-body">

                        @if ($errors->any())
                            <ul class="alert alert-danger">
                                @foreach ($errors->all() as $error)
                                    <li>{{ $error }}</li>
                                @endforeach
                            </ul>
                        @endif

                        {!! Form::model($assignment, [
                            'method' => 'POST',
                            'route' => ['assignments.update', $assignment->id],
                            'class' => 'form-horizontal',
                            'files' => true
                        ]) !!}
                        {!! Form::hidden('course_id', $course->id) !!}

                        @include ('admin.assignments.form', ['submitButtonText' => 'Update'])

                        {!! Form::close() !!}

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection