@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <div class="panel panel-default">
                    <div class="panel-heading">assignment {{ $assignment->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('admin/assignments/' . $assignment->id . '/edit') }}" class="btn btn-primary btn-xs" title="Edit assignment"><span class="glyphicon glyphicon-pencil" aria-hidden="true"/></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/assignments', $assignment->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<span class="glyphicon glyphicon-trash" aria-hidden="true"/>', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete assignment',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $assignment->id }}</td>
                                    </tr>
                                    <tr><th> Assignment </th><td> {{ $assignment->assignment }} </td></tr><tr><th> Description </th><td> {{ $assignment->description }} </td></tr><tr><th> Completed </th><td> {{ $assignment->completed }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection